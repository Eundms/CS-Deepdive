# 환경변수와 설정

## export, env, printenv
## PATH 설정
## .bashrc vs .bash_profile vs .zshrc
## /etc/environment, /etc/profile
## alias 설정

---

# alias (별명)

```shell
alias 별명='실제값'
```

- 예시: `alias ls='ls -F'`
- `type 명령어`를 이용하면 별명인지 확인 가능
- `unalias`를 이용하면 별명 삭제 가능
- 원본 명령어 실행: 전체 경로로 명령어를 실행 (`command 명령어` 또는 `\명령어`)

---

# 로케일 설정

## 현재 로케일 확인
```shell
locale
```

## 로케일 수정
```shell
sudo apt install language-pack-ko
sudo update-locale LANG=ko_KR.UTF-8
sudo dpkg-reconfigure locales  # 명령 후 ko_KR.UTF-8을 선택
```
